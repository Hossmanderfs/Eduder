// src/App.js — EduDer
// Punto de entrada de la app. La configuración de navegación
// está separada en navigation/AppNavigator.js para mayor claridad.
import React from 'react';
import AppNavigator from './navigation/AppNavigator';

export default function App() {
  return <AppNavigator />;
}
